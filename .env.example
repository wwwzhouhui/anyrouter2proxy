# AnyRouter Proxy 配置文件（透传模式）
# 复制此文件为 .env 并根据需要修改配置

# ==================== 上游服务 ====================
# AnyRouter 服务地址
# ANYROUTER_BASE_URL=https://anyrouter.top

# ==================== 服务端口 ====================
# Anthropic 协议代理端口 (anyrouter2anthropic.py)
# PORT=9998

# OpenAI 协议代理端口 (anyrouter2openai.py)
# OPENAI_PROXY_PORT=9999

# 绑定地址
# HOST=0.0.0.0

# ==================== 通用配置 ====================
# HTTP 请求超时时间（秒）
# HTTP_TIMEOUT=120

# 默认最大 token 数
# DEFAULT_MAX_TOKENS=8192

# ==================== OpenAI 代理专用 ====================
# 强制使用非流式后端（解决某些流式不稳定问题）
# FORCE_NON_STREAM=false

# 默认系统提示词
# DEFAULT_SYSTEM_PROMPT=You are Claude, a helpful AI assistant.

# ==================== 透传模式说明 ====================
# 本代理服务使用透传模式，不在服务端存储 API Key。
# 客户端必须在请求头中提供有效的 anyrouter.top API Key：
#
# OpenAI 协议 (anyrouter2openai.py):
#   Authorization: Bearer sk-your-api-key
#   或多 key 负载均衡: Authorization: Bearer sk-key1,sk-key2,sk-key3
#
# Anthropic 协议 (anyrouter2anthropic.py):
#   x-api-key: sk-your-api-key
#   或 Authorization: Bearer sk-your-api-key
#   多 key 负载均衡: x-api-key: sk-key1,sk-key2,sk-key3
#
# 如果未提供有效的 API Key，将返回 401 授权失败错误。
